# Create executable
Run `pyinstaller --windowed --onefile chrolispp_planner.py`. (To install pyinstaller, use an Anaconda environment. For example, `conda create -n pyinstaller-env python==3.13.11 pyinstaller==6.12.0`, then activate the environment with `conda activate pyinstaller-env`.)
